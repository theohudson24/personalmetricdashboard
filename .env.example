SUPABASE_DATABASE_URL="postgresql://runtime-pooler-connection"
SUPABASE_DIRECT_URL="postgresql://migration-connection"
FDC_API_KEY="DEMO_KEY"

# Add these after creating the Supabase project. The publishable key may be
# exposed to the browser; never expose the service-role key.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""

# Comma-separated emails allowed during private testing.
TEST_USER_EMAILS="your-email@example.com"
